**17:03** 🚀 `scrum-master` [session_start] Starting sprint: fix Voltron scaffolding so the scrum-master slash command is written to .claude/commands/scrum-master.md on a fresh scaffold, and ensure the auto-update hook self-heals a missing commands/ directory. Reported by a user whose fresh project got agents but no commands/ dir.
**17:08** → `scrum-master` [dispatch] Dispatching branch-manager to create the feature branch fix/scaffold-commands-dir off main, where the scaffold fix will land.
**17:11** → `scrum-master` [dispatch] Dispatching two harness-engineers in parallel: (T2) make the scaffold include the /scrum-master slash command for every project type, and (T3) make the auto-update hook repair a missing commands/scrum-master.md on projects that were already scaffolded with the bug. Neither commits.
